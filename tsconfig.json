{
  "compilerOptions": {
    /* Project Setup */
    "incremental": true,                                /* Enable incremental compilation for faster builds. */
    "tsBuildInfoFile": "./.tsbuildinfo",                /* Save .tsbuildinfo files to allow incremental compilation. */

    /* Language and Environment */
    "target": "ESNext",                                 /* Use the latest JavaScript features for emitted code. */
    "lib": ["ESNext", "DOM"],                           /* Include DOM and ESNext libraries for type support. */
    "module": "ESNext",                                 /* Use modern ECMAScript modules. */
    "moduleResolution": "node",                        /* Use Node.js-style module resolution. */
    "allowSyntheticDefaultImports": true,              /* Allow default imports from modules without a default export. */
    "esModuleInterop": true,                            /* Enable interoperability between ES Modules and CommonJS. */
    "forceConsistentCasingInFileNames": true,           /* Enforce consistent casing in module names. */

    
    /* Output Options */
    "outDir": "./JS",                                   /* Output directory for compiled files. */
    "rootDir": "./TS",                                  /* Root directory for source files. */
   

    /* Strict Type Checking */
    "strict": true,                                     /* Enable strict type checking options. */
    "noImplicitAny": true,                              /* Disallow variables of type `any`. */
    "strictNullChecks": true,                           /* Ensure null and undefined are handled explicitly. */
    "strictPropertyInitialization": true,               /* Ensure class properties are initialized properly. */
    "noImplicitThis": true,                             /* Warn when `this` is of type `any`. */
    "alwaysStrict": true,                               /* Always emit "use strict". */
    "strictBindCallApply": true,                        /* Check `bind`, `call`, and `apply` methods. */

  },
  "include": ["TS/**/*.ts"],                            /* Include all TypeScript files in the TS directory. */
   
}
